Resolved (2026-08-21) — verified against the current tree with `node tools/test/run.js` and spot-checked reads of the implementing code; deleted rather than left stale:
- The un-headed FAIL list and the "known cross-team mismatches after merging
  ws/ui-a + ws/content-core (2026-08-19)" block that used to live here (Slipstream/
  Quiet Cat, catch-ends-battle, bag items, capture animation, monster/perk/unlock
  rewards, brew buffs, dex recording, cat gaps, the Inventory pockets API, the
  'priority' battle effect) are all implemented and covered by green tests
  (test-content-core-cats.js, test-battle*.js, test-content-core-quests.js,
  test-dex-recording.js, test-region-*.js "cat gaps" cases). Nothing to do.

Browser QA 2026-08-20 (v2 @ integrated build, Firefox, 1411x1216):
- WORKS: title screen (night Cheshire, dish, rain, slots), name entry, difficulty picker, confirm dialog, overworld with real tiles/NPCs/clock HUD, virtual analogue stick moves the player diagonally, touch A/B/RUN/START, canvas fills the window at 60fps, no letterbox.
- BUG: on the name-entry screen the footer says "ESC Done" but pressing Escape does not commit the name (tapping DONE does). Fix in js/ui/title.js: Escape/START should confirm the entered name.
- NOTE: testers must hard-refresh once — the old service worker serves a stale index.html (cache name changed to mq2-*).

Browser QA 2026-08-20 (v2 @ f92ea55..53116e2, Firefox, 1280x800 and 1024x600)
FIXED this pass (see git log):
- name entry: ESC now commits the name (footer said "ESC Done" and it cancelled); the
  header chevron on that screen is now polled, it was drawn but dead.
- service worker: own-origin requests are network-first with the cache as the offline
  fallback; index.html calls registration.update() on load and on every return to
  visibility and reloads once when a new worker takes over. No more sticky builds.
- battle: every message and both HP panels said "null". engine.js read mon.nickname
  directly and MQ.Party.make (content/state.js) leaves nickname null, expecting the
  species name to stand in. All display paths now go through a new b.plainName().
- touch: the virtual stick and A/B/START/RUN pad were drawn over every menu (on top of
  the pause quick slots, inside the party panel) and ate taps there. Menu scenes now
  carry touchPad:false; core/input.js hides and disables the pad for them.
- settings: "Touch layout size", "Stick side", "Stick opacity" and "Vibration" called
  MQ.Input.setTouchScale/setTouchSide/setTouchAlpha/vibrateEnabled, none of which
  existed. core/input.js now implements them. A stale stored value (screenShake:true
  from an older build) printed raw as "true"; choice values are now sanitised on read.
- save cards: on a short card the party chips overprinted the map line ("MSILKINfield").
- toasts: walking into the canal four times stacked four identical bars; a repeat of the
  newest toast now just restarts its clock (core/ui.js).
- name entry: keyboard grid centred instead of hugging the top of a tall screen.
- settings were read from localStorage at load but only applied when you opened the
  Settings screen, so a cold boot ignored your volume, text speed and touch layout.
  UI.Settings now applies everything on the "boot" event.

STILL OPEN (not mine to fix / not small and safe):
- (nothing — the five QA bugs from this pass are fixed; see git log 2026-08-20:
  overworld zoom/letterbox, the Macclesfield angler, the save summary chapter,
  dex seen/caught recording, dialogue box top/bottom placement.)

After merging region-mid + region-southwest (2026-08-20):
- FIXED (2026-08-21): Region south-west's `aberaeron` fishing table `fish_aberaeron` was already defined in js/data/encounters_sw.js by the time this pass started — nothing left to do.
- FIXED (2026-08-21): duplicate trainer id `vex_3` (trainers_mid.js and trainers_sw.js both defined it). Neither is invoked by any script or map npc.trainer field — grepped every `battle({trainer:...})` call and every `trainer:` field in js/world/maps/*.js; only vex_1 (Ch.1), vex_2 (Ch.3) and vex_4 (Ch.8, trainers_nw.js — already correct, not the collision) are ever fought. Renamed region-southwest's copy to `vex_3_sw_unused` rather than reusing `vex_4` (that's a different, already-wired trainer) or deleting written content.

STILL OPEN — repeat-interaction sweep (2026-08-21, tools/test/test-npc-loops.js + tools/test/test-quest-flow.js):
- Chapter 5 ("Puppets on the Line") has no wired rival battle. The only VEX content at Crewe is js/story/npcs_mid.js's "mid_crewe_vex" scene (run by js/world/maps/mid_crewe.js's npc_crewe_vex), and it is dialogue-only — it never calls `C.battle`. trainers_mid.js's "vex_3" (full party/intro/win/lose/rematch data, clearly written for this exact platform-six scene) sits unused. DESIGN-INDEX's "vex_<n> (rival fights 1-4)" line and the region-mid workstream report both imply a Ch.5 rival fight was intended. Not fixed here — deciding where in the scene to insert the battle and matching its pacing/rewards to the other three rival fights is a content call, not a mechanical bug fix.
- region-southwest's own unused rival trainer (`vex_3_sw_unused`, see above) is in the same boat: full flavour text, no script anywhere battles it. Whoever picks up the Ch.5 rival fight should also decide whether this one is a leftover to delete or content for a different (unwritten) south-west beat.
